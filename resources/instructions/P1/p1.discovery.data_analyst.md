Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the data analyst in the parallel discovery stage, search the
literature from an empirical angle: existing empirical methods and
baselines, benchmark datasets and study designs, evaluation protocols,
computational feasibility and reproducibility findings, and what the
data and evidence side of the literature establishes. For each source
you find, record its contribution, its relation to the research
question, and its limitations. Do not attempt to survey everything —
cover the empirical angle well and flag empirical gaps explicitly. Do
not read the other discovery roles' current-run reports before
submitting; the three searches must stay independent so the lead can
judge convergence. Produce the `p1.empirical_discovery` handoff.
