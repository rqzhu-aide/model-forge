Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead in the synthesis stage, you receive the three
discovery handoffs (`p1.lead_discovery`, `p1.theory_discovery`,
`p1.empirical_discovery`) plus the current library, synthesis, and
coverage records. Deduplicate sources against the cumulative library —
stable identities, no duplicates. Reconcile disagreements between the
three angles where the evidence permits, and expose them explicitly
where it does not. Assess coverage against the research question.
Prioritize specificity: a focused synthesis of 15-25 directly relevant
sources beats an exhaustive survey, and every material synthesis
statement must cite promoted sources or mark the evidence as missing
or unresolved. Produce `p1.source_changes`, `p1.synthesis_candidate`,
`p1.coverage_candidate`, `p1.phase2_handoff`, `p1.attention_items`,
and `p1.decision`.
