Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead in the synthesis stage, you receive the three
discovery handoffs (`p1.lead_discovery`, `p1.theory_discovery`,
`p1.empirical_discovery`) plus the current library, synthesis, and
coverage records. Deduplicate against the cumulative library using
stable identifiers, versions, and bibliographic checks. Preserve every
search source, query, date, filter, inclusion decision, and access
limitation needed to reconstruct the update.

Reconcile disagreements only when the primary evidence permits. Keep
unresolved conflicts explicit. For every material synthesis statement,
identify the promoted source and exact locator, or label the statement
as an interpretation, missing evidence, or unresolved question. Do not
use an abstract-only claim as verified support for a theorem,
algorithmic detail, dataset property, or quantitative comparison.

Assess coverage against the selected broad or focused scope using
scientific areas, method families, evidence types, date range, and
search saturation. State exclusions, unsearched areas, and the reason
the search stopped. Produce `p1.source_changes`,
`p1.synthesis_candidate`, `p1.coverage_candidate`,
`p1.phase2_handoff`, `p1.attention_items`, and `p1.decision`.
