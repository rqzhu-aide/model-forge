{% if mode_id == "p2.full_catalog" %}
Mode-specific assignment: full-catalog empirical cross-review.
Review all three independent catalog proposals or revisions. Check
implementability, data requirements, study design, fair baselines, metrics,
tuning budget, uncertainty, compute and memory, numerical stability,
reproducibility, and empirical distinguishability.
{% elif mode_id == "p2.focused_method" %}
Mode-specific assignment: focused-method empirical cross-review.
Review all three analyses of the same selected stable method. Enforce the
one-method boundary and determine whether each proposed difference changes
the executable method, tuning, normalization, data needs, or evaluation.
Verify the no-change, editorial, or mathematical classification and its
reproducibility consequences.
{% elif mode_id == "p2.researcher_proposal" %}
Mode-specific assignment: researcher-proposal empirical cross-review.
Review the three independent evaluations of the same supplied method, not
three alternatives. Resolve disagreements about implementability, data,
fair tests, computational feasibility, uncertainty, stability, and the
smallest decisive experiment. Do not invent an alternative.
{% else %}
Review only the work authorized by the frozen Phase 2 mode.
{% endif %}
For every concern, record the affected object or claim, severity, evidence,
consequence, smallest correction, and recommended outcome in
unresolved_issues.

In addition, file structured per-method evaluations in method_evaluations:
one entry per catalog method, each keyed by the method's contracted
stable_id, with axis empirical_feasibility, a substantive assessment, and
issue_refs linking to your unresolved_issues entries. This field is
required - the lead's adjudication and the sealed method scores depend on
it. Produce p2.empirical_review.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
