{% if mode_id == "p2.full_catalog" %}
Mode-specific assignment: full catalog.
Search for a distinct empirical or computational mechanism, such as a new
use of data structure, regularization, optimization, approximation, or
evaluation-aware design, that could yield a scientifically meaningful
advantage. Do not mistake tuning or engineering variation for a new method.
Propose one credible catalog candidate or justified revision, or give a
documented no-new-option conclusion when no useful direction is supported.

Define all six canonical components and an implementable algorithm. Compare
the calculation with the closest formal Phase 1 work and strongest current
catalog method. State data requirements, fair baselines, metrics tied to the
target, tuning budget, uncertainty analysis, computational cost, failure
thresholds, and the experiment that could refute the claimed advantage.
Feasibility and testability take priority over novelty.
{% elif mode_id == "p2.focused_method" %}
Mode-specific assignment: focused method.
Audit and, if justified, revise only the selected stable method. Determine
whether the proposed change alters executable computation, tuning,
normalization, data requirements, or evaluation. Classify it as no change,
editorial, or mathematical and state the version, digest, reproducibility,
and downstream reassessment consequences. Do not create another method.
{% elif mode_id == "p2.researcher_proposal" %}
Mode-specific assignment: researcher proposal.
Independently evaluate the supplied researcher method without proposing an
alternative. Check implementability, data availability, fair comparison,
metrics, tuning burden, numerical stability, compute and memory cost,
uncertainty, reproducibility, and a decisive falsification experiment.
Separate missing specification from evidence that the method is infeasible.
{% else %}
Work only within the frozen Phase 2 mode and authorized method scope.
{% endif %}
Produce the p2.empirical_proposal handoff with quantified risks where
possible.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
