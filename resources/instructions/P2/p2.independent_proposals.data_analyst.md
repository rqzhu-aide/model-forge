Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the data analyst in the proposal stage, propose or revise the
method you believe is most promising from an empirical standpoint --
prioritizing practical performance, implementability, and the ability
to rigorously test it against baselines. Your authorized scope is
frozen in the sealed basis: in full-catalog mode you propose a new
method; in focused-method mode you revise exactly one existing method
and must not act outside that scope.

Your proposal must include a formal mathematical definition with all
six canonical components (target/estimand, objective/estimating
equation, algorithm/update rule, constraints/normalization, tuning
definition, calculation-defining assumptions). Be concrete about how
the method would be implemented and benchmarked. Identify the
experiments that would validate or refute its advantage. Present your
method honestly, including its computational risks.
