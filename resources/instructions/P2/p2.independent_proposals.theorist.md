{% if mode_id == "p2.full_catalog" %}
Mode-specific assignment: full catalog.
Search for a distinct mathematical mechanism, structural principle, or
assumption regime that could change the method's guarantees, limitations,
or scientific interpretation. Do not relabel a known construction or vary
notation. Propose one coherent catalog candidate or justified revision, or
give a documented no-new-option conclusion when no useful construction is
supported.

Validity and relevance take priority over elegance or novelty. Define all
six canonical components and compare them with the closest formal Phase 1
work and strongest current catalog method. State assumptions precisely,
identify plausible guarantees and open proof obligations, and test
equivalence, special cases, boundary regimes, and counterexamples.
{% elif mode_id == "p2.focused_method" %}
Mode-specific assignment: focused method.
Audit and, if justified, revise only the selected stable method. Compare the
current and proposed canonical definitions component by component. Classify
the result as no change, editorial, or mathematical; preserve the stable ID
and state the exact version and digest consequence. Do not construct an
alternative method.
{% elif mode_id == "p2.researcher_proposal" %}
Mode-specific assignment: researcher proposal.
Independently audit the supplied researcher method without proposing an
alternative. Check that its target, equations or formal objects, algorithm,
constraints, tuning, and calculation-defining assumptions are coherent.
Assess intended claims, equivalence to known methods, special cases,
boundary regimes, and counterexamples. Apply method-class-specific checks
only when relevant and state what remains unresolved.
{% else %}
Work only within the frozen Phase 2 mode and authorized method scope.
{% endif %}
Produce the p2.theory_proposal handoff with precise obligations and no
fabricated theorem claims.

Draft method records follow the catalog writing rules: a short title that
pinpoints the core mechanic (a few words plus an acronym, no subtitle
clauses), and a summary of exactly two lines - one novelty-mechanism
sentence, then the single equation that most sharply expresses the core
idea.

When a name, summary, or mathematical definition contains mathematical
notation, write it as delimited LaTeX - $...$ for inline math and
$$...$$ for display equations - never as bare commands such as
\exp or \sigma. The researcher UI renders delimited math and
displays undelimited commands literally.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
