{% if mode_id == "p2.full_catalog" %}
Mode-specific assignment: full-catalog reconciliation.
Reconcile the three proposals and two reviews into an auditable scoped
catalog change set. Keep genuinely distinct methods, merge cosmetic
variants, and drop nonviable proposals. For every retained method, produce
a complete method record with all six canonical components and literature
provenance. Verify each mechanism-level difference against the closest
formal Phase 1 work and current catalog before making a novelty statement.
Compare methods on validity, novelty confidence, scientific payoff,
feasibility, testability, decisive evidence, and principal risk.

When supported, present a balanced portfolio spanning the strongest
baseline or conservative extension, complementary moderate-risk options,
and a credible high-risk, high-value option. This is not a quota: do not
retain a weak, invalid, redundant, or untestable proposal for portfolio
balance. A catalog with no genuinely useful new option is a valid outcome;
record that conclusion and the evidence behind it. Do not choose a P3 or P4
branch for the user.
{% elif mode_id == "p2.focused_method" %}
Mode-specific assignment: focused-method reconciliation.
Reconcile all reports into at most one change to the selected stable method.
Do not add, merge, retire, or alter another method. Decide among no change,
editorial change, and mathematical change. For editorial change, preserve
the version and definition digest. For mathematical change, preserve the
stable ID, identify every canonical-definition difference, advance the
version by exactly one, compute a new definition digest, and state affected
P3, P4, and P5 records. Emit no method record when no replacement is
warranted.
{% elif mode_id == "p2.researcher_proposal" %}
Mode-specific assignment: researcher-proposal reconciliation.
Reconcile the independent evaluations and cross-reviews of the single
researcher proposal. Decide: viable now, viable after specified revision,
insufficient evidence, or not viable. Do not propose an alternative. Only
for a viable-now, sufficiently specified, distinct method, produce one
complete new method record with initial lineage and all six canonical
components. Otherwise emit no method record and state the smallest
correction or evidence needed.
{% else %}
Reconcile only the work authorized by the frozen Phase 2 mode.
{% endif %}

Disposition every material reviewer concern explicitly. Use only formal
Phase 1 sources for promoted literature claims. For a directly relevant
missing paper, dataset, or baseline, create a reassessment_required
attention item whose question begins with LITERATURE_GAP:. Produce
p2.method_changes, p2.attention_items, and p2.decision. The decision must
state the outcome, reasons, principal uncertainty, principal risk, and the
smallest result that would change it.
