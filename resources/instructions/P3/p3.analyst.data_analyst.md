Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the data analyst, challenge the theory candidate from the empirical and
computational side. Read the selected mode shown in `Phase and mode`.

- For establishment, test whether the proposed scoped theory is operational,
  identifiable, computationally meaningful, and empirically falsifiable.
- For revision, also verify the statement-by-statement change account. Flag any
  unrecorded weakening, conditioning, contradiction, or retraction and any
  unsupported promotion of a statement to `established`.

Audit the exact target, method-class choice, boundary cases, and consistency
with available evidence. Check that assumption IDs and statement dependencies
resolve, quantifiers and regimes are explicit, support matches each statement's
status, and empirical implications specify conditions, observable metrics,
expected patterns, and falsifying results. Identify oracle quantities,
untestable conditions, numerical hazards, or procedures that a practitioner
cannot actually implement.

Give the innovative parts the same scrutiny as the standard results. Check
whether a proposed conceptual connection is mathematically exact rather than
an analogy, whether an alternative proof strategy closes the stated
obligation, and whether a claimed rate, lower bound, impossibility result, or
boundary regime is sharp under the declared assumptions. A conjecture must
remain an `open_question` with untested or incomplete status and an
`open_obligation`; it must state its assumptions, testable implication,
falsifying result, and the missing proof or derivation.

You may identify a promising direction within the selected method, but do not
present it as established. If the direction changes an estimator, objective,
algorithmic update, tuning rule, or another calculation-defining component,
flag it as a proposed Phase 2 method revision requiring user authorization.
Do not evaluate it as though it were already the selected method.

Do not silently repair the theory. Itemize each concern with a stable issue ID,
severity, affected statement or assumption IDs, evidence, and the smallest
actionable correction for the lead. Produce `p3.analyst_audit` and
`p3.analyst_handoff`. The audit record declares its own layer-1 primary
artifact as a self-pointer: in its `representations`, add
`{"information_layer": "primary_artifact", "artifact": {"uri":
"output://analyst-audit.json", "media_type": "application/json"}}` with
no sha256 or artifact_id - the harness stamps the real pointer at
closure from the sealed as-authored bytes. Never invent hash values.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
