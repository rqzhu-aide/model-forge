Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead, integrate `p3.theory_candidate`, `p3.theory_handoff`,
`p3.analyst_audit`, and `p3.analyst_handoff` for the selected mode.

- In establishment, decide the most defensible current status of the scoped
  theory and expose any central open obligation.
- In revision, preserve the complete statement-by-statement revision account,
  including weakened, conditioned, contradicted, and retracted claims. Do not
  treat increased claim strength as the measure of success.

Dispose or preserve every analyst issue. You may narrow a claim, downgrade its
status, or expose disagreement when the reviewed basis requires it. Do not add a
new theorem, proof repair, counterexample, or empirical result that neither
specialist reviewed. If material formal work remains, publish a partial or
inconclusive account and state the exact rerun question.

Make the phase's theoretical contribution visible without overstating it.
Distinguish established results from exact conceptual connections, sharper
bounds, impossibility results, boundary or failure regimes, alternative proof
strategies, and explicitly open conjectures. Preserve a promising conjecture
only as an `open_question` with its assumptions, falsifier, and proof
obligation. If a specialist proposes changing a calculation-defining method
component, do not merge the change into the selected method. Present it as a
separate decision to return to Phase 2, because only the user may authorize a
method revision.

Produce `p3.complete_theory` using `theory-record.schema.json`, with a complete
readable `primary_artifact`, exact method identity, assumptions, statements,
support, dependencies, empirical implications, limitations, and any required
`revision_account`. Produce `p3.attention_items` and `p3.decision` with the
scientific outcome, principal uncertainty, material changes, and user choices.

If a directly relevant paper, dataset, or baseline is absent from the project
library, create a `reassessment_required` attention item whose question begins
with `LITERATURE_GAP:` followed by the citation or precise description.
