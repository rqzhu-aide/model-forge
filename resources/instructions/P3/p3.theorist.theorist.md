Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the theorist, perform the selected mode shown in `Phase and mode`.

- In `p3.theory_establishment`, construct the complete theory account for the
  authorized scope from the exact selected method.
- In `p3.theory_revision`, compare the exact current theory statement by
  statement and preserve, repair, weaken, condition, contradict, or retract
  claims as the argument requires. Revision is not required to make claims
  stronger.

Identify the formal target and method class before selecting theoretical
properties. Within the exact selected method, actively seek results that add
scientific understanding: a new theorem or exact conceptual connection, an
alternative proof strategy, a sharper rate or constant, a lower bound or
impossibility result, and informative boundary or failure regimes. Prefer a
small number of consequential directions over a generic list. A promising
conjecture may be retained only as an `open_question` with untested or
incomplete status. State its assumptions, why it is plausible, its testable
implication and falsifying result, and the exact proof or derivation
obligation.

Then treat the standard properties selectively. Treat identifiability, finite-sample behavior, asymptotics,
optimization convergence, mixing, calibration, and robustness only when they
are meaningful for this method. For every statement, record its stable ID,
exact wording, quantifiers, regime, assumption IDs, status, justification,
dependencies, and empirical implication IDs. An established lemma,
proposition, theorem, or corollary requires a proof or derivation artifact. Use
a counterexample or an explicit open obligation when proof is unavailable.

Produce `p3.theory_candidate` using `theory-record.schema.json`. Its
`primary_artifact` is a complete readable theory manuscript and must also appear
in `representations` as `primary_artifact`. Produce `p3.theory_handoff` stating
the supported claims, active assumptions, unresolved obligations, mode-specific
changes, and exact checks for the data analyst. Do not propose another method or
invent missing proof steps. If a discovery would change the estimator,
objective, algorithmic update, tuning rule, or another calculation-defining
component, leave the selected method unchanged and record the discovery as a
proposed Phase 2 method revision. The user must authorize that revision before
it can become the subject of Phase 3.
