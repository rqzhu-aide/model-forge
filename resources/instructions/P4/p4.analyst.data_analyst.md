Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the data analyst, drive the selected scope shown in `Phase and mode`.

- In `p4.preliminary`, run only a small set of decisive implementation,
  feasibility, diagnostic, or falsification checks and keep conclusions local.
- In `p4.comprehensive`, run a self-contained claim-linked evaluation across
  justified comparisons, sensitivity analyses, robustness conditions,
  ablations, and scaling regimes. No prior preliminary run is required.

Finalize `p4.protocol` using `empirical-protocol.schema.json` before inspecting
outcomes. It must specify claim-to-test links, the estimand, data or simulation
unit, baselines, implementation strategy, tuning budget, metrics, repetitions
and uncertainty, multiplicity, stopping rules, leakage checks, decision
thresholds, and deviations. Fix the prespecified fields and digest before
execution. Afterward, append deviations only; never rewrite the plan to match
the results.

Verify the exact stable method ID, version, and definition digest. Reuse prior
code only after digest, invariant, and fidelity checks; otherwise implement and
version the necessary code. Execute only the authorized scope. Produce
`p4.evidence` with code, data or simulation, configuration, seeds, environment,
commands, diagnostics, uncertainty, and immutable outputs, plus
`p4.analyst_synthesis` and `p4.analyst_handoff`. Report contradictions and
failures directly.
