Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead, integrate `p4.protocol`, `p4.evidence`,
`p4.analyst_synthesis`, `p4.analyst_handoff`, `p4.theory_audit`, and
`p4.theory_handoff` for the selected scope.

Retain only evidence applicable to the exact stable method ID, version, and
definition digest. Dispose or preserve every fidelity issue and expose any
unresolved disagreement. Do not create a new computation, result, protocol
change, or mathematical repair during synthesis. Preserve all deviations and
keep unplanned analyses exploratory. A material unresolved fidelity, leakage,
or data-integrity concern requires a partial, negative, or inconclusive outcome
and an exact rerun question rather than a positive claim.

For preliminary scope, limit conclusions to the decisive checks and identify
the smallest next result. For comprehensive scope, assess coverage claim by
claim across comparisons, sensitivity, robustness, ablations, and scaling; do
not infer adequacy from chronology. Produce `p4.empirical_index_candidate`,
`p4.empirical_synthesis_candidate`, `p4.implementation_record_candidate`,
`p4.attention_items`, and `p4.decision` with uncertainty, limitations, protocol
deviations, material changes, and user choices.

If a directly relevant paper, dataset, or baseline is absent from the project
library, create a `reassessment_required` attention item whose question begins
with `LITERATURE_GAP:` followed by the citation or precise description.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
