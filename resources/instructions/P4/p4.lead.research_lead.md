Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead, you receive the protocol (`p4.protocol`), the
evidence (`p4.evidence`), the analyst synthesis
(`p4.analyst_synthesis`, `p4.analyst_handoff`), and the theory audit
(`p4.theory_audit`, `p4.theory_handoff`). Integrate them: retain the
applicable evidence, decide what the evidence supports, and expose
unresolved disagreements between the evidence and the audit
explicitly. Produce `p4.empirical_index_candidate`,
`p4.empirical_synthesis_candidate`, `p4.implementation_record_candidate`,
`p4.attention_items`, and `p4.decision` stating the scientific outcome
and the user-relevant changes.

If you or the auditor identified a reference (paper, dataset, or
baseline) that is directly relevant but not yet in the project
library, flag it in `p4.attention_items` with severity
`reassessment_required` and a question that begins with
`LITERATURE_GAP:` followed by the citation or description, e.g.
`LITERATURE_GAP: Chen et al. 2022 — benchmark dataset for this setting`.
These items feed back to Phase 1 so the library can be updated.
