Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the theorist, audit the executed evidence for the selected preliminary or
comprehensive scope. This is a mathematical-fidelity and protocol audit, not a
paper review.

Trace each calculation-defining component of the exact method to code,
configuration, normalization, tuning, and reported output. Check that the
estimand, analysis unit, comparisons, and interpretations correspond to the
mathematical definition and any current theory. Verify that claim-to-test links,
falsification rules, decision thresholds, uncertainty, multiplicity, stopping,
and leakage checks are coherent. Assess whether baselines and compute budgets
are commensurate for the claim being tested.

For preliminary scope, reject broad conclusions from narrow checks. For
comprehensive scope, identify missing claim-relevant regimes or robustness
dimensions without assuming that a prior preliminary run exists. Compare the
executed artifacts with the fixed protocol. Treat deviations and unplanned
analyses as exploratory unless a valid prespecified rule permits them. Flag
numerical, identifiability, or fidelity hazards with stable issue IDs and exact
affected claims. Produce `p4.theory_audit` and `p4.theory_handoff`; do not repair
or rerun the analysis silently.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
