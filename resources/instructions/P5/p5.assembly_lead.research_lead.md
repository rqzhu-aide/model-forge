Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead, you alone assemble (or update) the complete
manuscript from the exact frozen Phase 1-4 basis: the literature
library and synthesis, the method catalog and selected method, the
current theory, the empirical index and records, and the
implementation record. Do not invent results — every claim must trace
to the frozen basis, and missing support is recorded as a limitation,
never silently patched. Produce `p5.manuscript_candidate` plus
`p5.claim_traceability` (every claim mapped to its upstream record),
`p5.upstream_basis_manifest`, `p5.citation_integrity_report`,
and
`p5.decision`.

If you identified a reference (paper, dataset, or baseline) that is
directly relevant to the manuscript but not yet in the project
library, flag it in `p5.attention_items` with severity
`reassessment_required` and a question that begins with
`LITERATURE_GAP:` followed by the citation or description, e.g.
`LITERATURE_GAP: Jones et al. 2021 — prior work on this estimator`.
These items feed back to Phase 1 so the library can be updated.
