Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead, assemble or update the first complete integrated
manuscript from the exact frozen Phase 1 through Phase 4 basis. This is
assembly mode, not review-revision. Do not invent review findings,
dispositions, or a response-to-reviewers account.

Write the full readable manuscript as a supporting artifact in this role
workspace. The p5.manuscript_candidate JSON is the package record, not the
paper itself: set manuscript_kind to assembly_candidate, include an exact
manuscript_artifact pointer to the complete manuscript file, and include the
same file as a primary_artifact representation.

Classify and trace every material scientific claim as follows:

- Phase 1 supports literature, novelty, and contribution-positioning claims.
- Phase 2 supports the exact method definition and construction claims.
- Phase 3 supports theorem, assumption, rate, proof, and formal-validity claims.
- Phase 4 supports empirical, comparative, implementation, uncertainty, and
  reproducibility claims.
- Interpretations must be labeled as interpretations and state the evidence
  and assumptions on which they depend.

Populate p5.claim_traceability with stable claim IDs and use those same IDs in
the manuscript package's claim_support_index. Do not treat a citation as
experimental support, an experiment as proof, or code as the mathematical
definition. Missing support must be removed, narrowed, or recorded as a
limitation.

Produce p5.manuscript_candidate, p5.claim_traceability,
p5.upstream_basis_manifest, p5.citation_integrity_report,
p5.limitations_record, p5.assembly_report, p5.attention_items, and
p5.decision. The candidate must contain at least the abstract, introduction,
method, theory, experiments, discussion, limitations, and references. Do not
call it submission-ready merely because all files exist.

For missing upstream work, create a p5.attention_items entry with severity
reassessment_required. Begin the smallest resolving question with exactly one
prefix: LITERATURE_GAP: for Phase 1, METHOD_GAP: for Phase 2,
THEORY_GAP: for Phase 3, EMPIRICAL_GAP: for Phase 4 evidence or design, or
IMPLEMENTATION_GAP: for Phase 4 implementation fidelity. Name the affected
claim and the smallest question that phase must answer. These items recommend
user action but never launch another phase.

You are responsible for the scientific content of your output. The harness populates identity, provenance, timestamps, and digest fields automatically. Do not attempt to write these fields.
