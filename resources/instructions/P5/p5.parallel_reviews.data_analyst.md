Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the data analyst in the parallel review stage, audit one immutable
manuscript snapshot. Check that every reported number, comparison, uncertainty
statement, and reproducibility claim matches the empirical index, evidence,
implementation record, and exact code, data, command, seed, environment, and
metric definitions available in the frozen basis.

Read the current theory at the implementation interface. Verify that the code
implements the estimator and assumptions to which the stated guarantee
applies, and that the experiments evaluate that same object. Distinguish
sampling uncertainty, algorithmic randomness, approximation error, and
computational error. Report invalid comparisons, leakage, tuning asymmetry,
unstated exclusions, or unsupported generalization.

Produce p5.empirical_audit as an array of open review findings ordered by
scientific consequence. Every item must have a stable issue_id, a specific
location, evidence basis, calibrated severity and confidence, scientific
consequence, resolution class, and status exactly open. Do not add a lead
disposition, disposition reason, or revision location. You are diagnosing the
frozen manuscript, not revising it. If no issue is warranted, produce an empty
array rather than inventing criticism.
