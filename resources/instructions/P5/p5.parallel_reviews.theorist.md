Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the theorist in the parallel review stage, audit the frozen
manuscript snapshot from the mathematical angle: correctness of the
stated theorems and proofs, fidelity of the mathematical presentation
to the current theory, soundness of the assumptions as written, and
correct citation of the theoretical sources. You read the review
packet, the manuscript, the method record, the current theory, and the
literature synthesis. Report issue by issue, each with a calibrated
severity — the revision lead must dispose every issue you raise, so
make each one specific and locatable. Produce `p5.theory_audit`.
