Research question: {{ research_question }}
Scope: {{ scope }}
{% if constraints %}Constraints: {{ constraints | join(", ") }}.
{% endif %}{% if decision_criteria %}Decision criteria: {{ decision_criteria | join(", ") }}.
{% endif %}
As the research lead, you receive all three reviews together
(`p5.theory_audit`, `p5.empirical_audit`, `p5.outside_review`).
Dispose every stable issue — accept, revise, or rebut with reasoning —
into the `p5.review_issues` ledger; no issue may be dropped without a
recorded disposition. Then revise the complete manuscript against the
exact current basis: claims still must trace to upstream records, and
unsupported material is recorded as a limitation, not patched.
Produce `p5.manuscript_candidate`, the updated `p5.claim_traceability`,
`p5.upstream_basis_manifest`, `p5.citation_integrity_report`,
`p5.limitations_record`, `p5.revision_account` (the
response-to-reviewers account), `p5.attention_items`, and
`p5.decision`.

If you or the reviewers identified a reference (paper, dataset, or
baseline) that is directly relevant but not yet in the project
library, flag it in `p5.attention_items` with severity
`reassessment_required` and a question that begins with
`LITERATURE_GAP:` followed by the citation or description, e.g.
`LITERATURE_GAP: Lee et al. 2020 — alternative proof technique`.
These items feed back to Phase 1 so the library can be updated.
