# Base Hermes profile configuration for the research lead.
# Managed by Model Forge configuration. Do not edit inside a run profile.

profile:
  display_name: Research lead
  role: research_lead

memory:
  persistence: persistent
  scope: project

behavior:
  temperature: 0.4
  max_tokens: 4096
