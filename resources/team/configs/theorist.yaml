# Base Hermes profile configuration for the theorist.
# Managed by Model Forge configuration. Do not edit inside a run profile.

profile:
  display_name: Theorist
  role: theorist

memory:
  persistence: persistent
  scope: project

behavior:
  temperature: 0.3
  max_tokens: 8192
